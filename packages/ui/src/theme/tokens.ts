/**
 * 视觉设计 Tokens — 全局统一配置入口
 *
 * 修改颜色等视觉属性时，只需改动此文件。
 * CSS 侧对应的值定义在 src/theme/tokens.css，请保持两侧数值一致。
 *
 * 使用场景：
 *   - theme.ts：读取默认主色值
 *   - App.vue / AntDesign themeConfig：读取 colorPrimary 等 token
 */

export const designTokens = {
  color: {
    // ── 品牌主色 ──────────────────────────────────────────────────────
    /** 亮色模式主色 */
    primary: '#E87F24',
    /** 暗色模式主色（偏浅，保证深色背景下的对比度） */
    primaryDark: '#f8b88a',
    /** 主色悬停 / 加深态 */
    primaryHover: '#c36814',

    // ── 语义色 ───────────────────────────────────────────────────────
    /** 成功 / 绿色 */
    success: '#52c41a',
    /** 警告 / 橙色 */
    warning: '#faad14',
    /** 错误 / 危险 */
    error: '#ff4d4f',
    /** 强调 / 紫色 */
    accent: '#722ed1',

    // ── 页面背景（亮色模式）──────────────────────────────────────────
    /** 页面底层背景 */
    surface: '#ffffff',
    /** 卡片 / 容器背景 */
    panel: '#f9fafb',

    // ── 页面背景（暗色模式）──────────────────────────────────────────
    /** 页面底层背景（暗色） */
    surfaceDark: '#111827',
    /** 卡片 / 容器背景（暗色） */
    panelDark: '#1f2937',

    // ── 正文文字 ─────────────────────────────────────────────────────
    /** 正文颜色（亮色） */
    text: '#111827',
    /** 正文颜色（暗色） */
    textDark: '#f3f4f6',
  },
} as const