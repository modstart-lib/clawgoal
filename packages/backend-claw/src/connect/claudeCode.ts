/**
 * @deprecated Claude Code 已迁移至 ACP stdio 方式（acpBackends.ts 中的 claudeCode 条目）。
 * 此文件仅保留以供历史参考，不再使用。
 */
export const name = 'claudeCode'
export const title = 'Claude Code'

/** ACP 模式启动命令：claude --experimental-acp */
export const acpCmd = 'claude'
export const acpArgs = ['--experimental-acp']
